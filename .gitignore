# macOS
.DS_Store
**/.DS_Store

# Wrangler / Cloudflare
.wrangler/
.dev.vars
*.env
.env*
# Lokale wrangler.toml mit echten Cloudflare-Resource-IDs nicht committen.
# Forks/Contributors kopieren wrangler.toml.example → wrangler.toml + eigene IDs.
wrangler.toml

# Node
node_modules/
npm-debug.log*

# Editor / Tools
.vscode/
.idea/
.claude/

# DB Backups (D1 snapshots — können User-Daten enthalten, NIE committen)
backup-*.sql
*.bak
*.bak.*

# Vite / Svelte build output
frontend/dist/
frontend/bundle-stats.html
.vite/
vite.config.js.timestamp-*.mjs
*.timestamp-*.mjs

# Press-kit build artifacts (generated by scripts/build-press-kit.js via deploy.sh)
frontend/public/press/index.html
frontend/public/press/renex-press-kit.zip
frontend/public/press/_staging/
# Raw video masters (encoded outputs are committed; raws kept local only)
assets/press/_sources/

# Internes Launch-Playbook (Strategie/Runbook/Known-Issues) — lokal behalten, NICHT public
docs/LAUNCH.md
docs/archive/LAUNCH.md

# Interner Krypto-Migrations-Bauplan (legt aktuelle Krypto-Schwächen offen) — NIE public
docs/CRYPTO_PQ_SIGNAL_BUILDPLAN.md

# Interner eGov-Bauplan (Strategie/Roadmap) — NIE public
docs/BUILDPLAN_EGOV.md

# Vision/Strategie (Marktanalyse, Personas, Roadmap, Decision Logs) — seit
# 2026-07-26 intern. Der README trägt die öffentliche Kurzfassung.
# ⚠️ Die früheren Versionen bleiben in der Git-HISTORIE öffentlich lesbar —
# das Ausklinken verhindert nur künftige Preisgabe, keine rückwirkende.
docs/VISION.md

# Wiedereinstiegs-Runbook (Betriebs-Hebel, Onboarding, Roadmap-Stand) — intern
docs/RUNBOOK_INTERN.md
